options:

steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
  - run
  - deploy
  - product-sync
  - --image
  - gcr.io/$PROJECT_ID/product-sync:$SHORT_SHA
  - --region
  - us-central1
  - --platform
  - managed
